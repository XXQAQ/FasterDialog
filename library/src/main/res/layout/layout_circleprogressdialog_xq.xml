<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/colorWidely"
    >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:divider="@drawable/spacing_medium"
        android:showDividers="middle"
        android:paddingTop="@dimen/spacing_max"
        android:paddingBottom="@dimen/spacing_more"
        >

        <pl.droidsonroids.gif.GifImageView
            android:id="@+id/iconView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:visibility="gone"
            />

        <TextView
            android:id="@+id/titleView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:paddingLeft="@dimen/spacing_medium"
            android:paddingRight="@dimen/spacing_medium"
            android:text="提醒"
            android:textColor="@color/text_strong"
            android:textSize="@dimen/text_max"
            android:textStyle="bold"
            android:lineSpacingMultiplier="1.2"
            />

        <TextView
            android:id="@+id/contentView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:gravity="center"
            android:paddingLeft="@dimen/spacing_medium"
            android:paddingRight="@dimen/spacing_medium"
            android:text="立即充值,只有充值才能变得更强"
            android:textColor="@color/text_normal"
            android:textSize="@dimen/text_more"
            android:lineSpacingMultiplier="1.2"
            />

    </LinearLayout>

    <ProgressBar
        android:id="@+id/progressBar"
        android:layout_width="@dimen/icon_xmax"
        android:layout_height="@dimen/icon_xmax"
        android:layout_marginBottom="@dimen/spacing_min"
        android:layout_gravity="center_horizontal"
        android:indeterminateTint="@color/colorPrimary"
        />

</LinearLayout>